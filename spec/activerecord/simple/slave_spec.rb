RSpec.describe Activerecord::Simple::Slave do
  it "has a version number" do
    expect(Activerecord::Simple::Slave::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
